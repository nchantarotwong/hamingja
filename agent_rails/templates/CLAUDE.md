# CLAUDE.md

See [`AGENTS.md`](./AGENTS.md) for the workflow rails this repository uses.
Claude Code reads `CLAUDE.md` automatically; the actual instructions live in
`AGENTS.md` so they are shared with other harnesses (Codex etc.).

Claude-specific notes (add only if you have any; otherwise leave this file as
a pointer):

<!-- e.g. "Run `pnpm test:watch` in another terminal while working on the UI." -->
